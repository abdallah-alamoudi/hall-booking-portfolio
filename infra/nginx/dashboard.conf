# Dashboard static hosting example (SPA fallback)
server {
  listen 80;
  server_name dashboard.example.com;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
