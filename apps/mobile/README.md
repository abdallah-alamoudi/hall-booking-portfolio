# Mobile App Placeholder (Flutter)

This folder is reserved for the Flutter customer app.

Scaffold intentionally keeps mobile code as-is for now.

Suggested next steps:

1. Initialize Flutter app in this folder (`flutter create .`).
2. Configure API base URL and env strategy.
3. Generate API client models from `packages/contracts/openapi.yaml`.
